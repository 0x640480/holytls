# Fetched / generated on demand by scripts/ (regenerated, not committed).
# Pinning files ARE committed: server/caddy.sha256, server/payloads/CHECKSUMS,
# go.mod/go.sum, Cargo.toml/Cargo.lock.
server/caddy
server/caddy.log
server/caddy.pid
server/payloads/*.bin

# Self-signed local test certs (regenerated by scripts/gen_cert.sh).
config/ca.pem
config/ca.key
config/leaf.pem
config/leaf.key
config/*.srl

# Built harnesses + Rust target.
c/bench_holytls
go/tls_client/bench_tls_client
go/httpcloak/bench_httpcloak
rust/wreq/target/

# Raw per-run results (a curated sample lives at SAMPLE_REPORT.md).
results/
